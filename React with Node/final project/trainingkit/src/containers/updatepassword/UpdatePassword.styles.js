import styled from 'styled-components';
import { rem } from 'components/styles';

export const UpdatePasswordStyled = styled.div``;

export const Input = styled.div`
  margin-bottom: ${rem(14)};
`;

UpdatePasswordStyled.Input = Input;
